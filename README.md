# PythonImport
Import in only 2-3 lines!

## Syntax
The import() function looks for a tuple so that it can import each item one by one
### Example
``` python
import PythonImport
# Put in your modules like os or sys
PythonImport.import(("os", "sys"))
```
